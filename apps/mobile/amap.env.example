# 高德开放平台 SDK Key。复制本文件为 amap.env 后填入真实值。
# amap.env 已被 .gitignore 排除，禁止提交真实 Key。
#
# Key 与「PackageName + 签名 SHA1」绑定，调试与发布指纹需分别在高德控制台登记：
#   PackageName   com.savorseek.app
#   Debug  SHA1   见 docs/开发日志.md
#   Release SHA1  见 docs/开发日志.md
#
# 注入方式（两端不同）：
#   Android  由 android/app/build.gradle.kts 在构建期读取本文件，注入
#            AndroidManifest 的 com.amap.api.v2.apikey。原生 SDK 直接读取该
#            meta-data，因此 IDE 运行、flutter run、flutter build 均生效，
#            无需附加任何命令行参数。
#   iOS      无等价机制，Key 只能经 Dart 注入，需在构建时附加
#            --dart-define-from-file=amap.env。
AMAP_ANDROID_KEY=
AMAP_IOS_KEY=
