<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 高德地图瓦片加载所需的最小权限集合。
         定位相关权限（ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION）当前未声明：
         本阶段只展示瓦片与手势，未启用定位图层。启用定位时再按需补充，
         避免过度申请权限。 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- usesCleartextTraffic：自建 Supabase 当前仅提供 http://，Android 9+
         默认拦截明文请求，不声明会让 SDK 直接报连接失败，容易被误判为服务端
         故障。这是开发期妥协——anon key、密码与 JWT 均明文传输，可被中间人
         截获。服务端配好域名与 HTTPS 后必须移除本属性。 -->
    <application
        android:label="savorseek"
        android:name="${applicationName}"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher">
        <!-- 高德地图 SDK Key。值由 app/build.gradle.kts 从 amap.env 读取后
             以 manifestPlaceholders 注入，Key 本身不出现在版本库中。
             原生 SDK 直接读取该 meta-data，因此任意启动方式均生效。 -->
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="${amapApiKey}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
