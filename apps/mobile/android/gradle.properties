org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin incremental caches cannot relativize plugin sources across C: and F:.
# Keep Android debug/release builds deterministic on this Windows layout.
kotlin.incremental=false
# The project is on F: while pub-hosted Kotlin sources are on C:. Disable
# incremental Kotlin path-relativization, which fails across Windows drives.
kotlin.incremental=false
