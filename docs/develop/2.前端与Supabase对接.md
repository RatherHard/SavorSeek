# 前端与 Supabase 对接

本文档面向接手该任务的开发者/Agent，说明当前对接状态、服务端已就绪的能力、需要补齐的客户端代码，以及验证方式。

## 当前状态：读取链路已对接，写入已封装且部分入口已落地

截至 2026-08-28，行程数据库已按全部迁移顺序应用到自建实例；本节只记录当前仍适用的对接事实。已实现并验证的部分：

| 能力 | 实现位置 | 状态 |
|------|----------|------|
| 客户端初始化 | `lib/app/supabase/supabase_bootstrap.dart` | 已实现，`main()` 在 `runApp` 前 await |
| 依赖注入 | `lib/app/app_dependencies.dart` | 已实现，贯穿 `SavorSeekApp` → `AppShell` → 各页面 |
| 邮箱认证 | `lib/features/auth/auth_service.dart`、`auth_sheet.dart` | 已实现，入口在「我的」页「账号管理」 |
| 只读查询 | `SupabaseTripRepository.loadPlan()` | 已实现，单次嵌套查询 |
| 写入 RPC | `createTrip` / `addTripDay` / `addTripItem` 及节点、行程生命周期 RPC | 已封装；创建、添加、编辑、改期、删除、完成和取消路径已接入相应客户端能力 |
| 字段映射 | `lib/features/trip/trip_mapper.dart` | 已实现，含时区与枚举转换 |

`flutter analyze` 与当前 Flutter 测试均应作为验证入口。自建实例的最终 schema 还需以已应用迁移和 `supabase/snippets/verify_trip_page_consolidation.sql` 的核对结果为准。

下文保留基础 RPC 签名、读写边界和实施过程，涉及已被后续迁移替换的旧状态时，以上述最终 schema 为准。

## 服务端已就绪的能力

自建 Supabase 部署在 `/opt/savorseek`，Kong 网关对外暴露 `8000` 端口。以下均已实测通过：

| 能力 | 端点 | 实测结果 |
|------|------|----------|
| PostgREST | `:8000/rest/v1/` | 带 apikey 200，无 apikey 401 |
| Auth (GoTrue) | `:8000/auth/v1/` | 注册可签发有效 JWT |
| Edge Function | `:8000/functions/v1/agent` | 返回 `{"ok":true,"function":"agent"}` |
| Realtime | `:8000/realtime/v1/websocket` | WebSocket 握手 101 |
| Storage | `:8000/storage/v1/` | 带 apikey 200 |

已验证的完整业务链路（curl）：注册 → 获得 JWT → 查 `trips` 返回 `[]`（RLS 生效）→ 用 anon key 直查返回 401。客户端读写链路及行程页相关入口已在后续迭代接入；本文件后面的实施步骤保留为历史实现记录。

数据库侧：`trips` / `trip_days` / `trip_items` / `trip_idempotency_keys` 四张行程相关表已应用。当前公开给 `authenticated` 的行程写入 RPC 为 11 个：`create_trip`、`add_trip_day`、`add_trip_item`、`edit_trip_item`、`reschedule_trip_item`、`update_trip_item`、`delete_trip_item`、`batch_delete_trip_items`、`complete_trip`、`cancel_trip`、`delete_trip`；校验与守卫函数不计入客户端 RPC。核心表读取策略仍为 3 条，`auth.users` 存在。

## 最终行程结构补充

最终迁移链（`20260821000002` 至 `20260827000013`）已在自建实例应用，并由 `supabase/snippets/verify_trip_page_consolidation.sql` 对账通过。实现时须注意以下已生效的最终行为：

- `trip_items.status` 仅允许 `planned`、`completed`、`skipped`；节点级 `cancelled`、`cancel_trip_item`、`restore_trip_item` 和 `batch_cancel_trip_items` 已移除。
- `trip_items_active_position_uq` 是 `(trip_day_id, position)` 的无条件唯一索引，不再排除取消项。
- `trips.timezone` 仍是必填有效 IANA 标识，但行程创建后不可修改；`change_trip_timezone` 已删除。
- 行程状态仍允许 `draft`、`confirmed`、`in_progress`、`completed`、`cancelled`，并额外允许 `draft -> completed` 与 `confirmed -> completed` 的直接完成路径。
- 已完成行程仍禁止 `add_trip_day`、`add_trip_item`、节点编辑、改期、删除和批量删除；行程本身仍可通过 `delete_trip` 删除。已取消行程不受该只读守卫限制，但其具体状态转换仍由行程触发器和 RPC 校验。


## 关键契约：读写路径不对称

参见 `supabase/migrations/20260821000002_itinerary_schema.sql` 及其后续迁移：

```sql
revoke all on public.trips, public.trip_days, public.trip_items,
  public.trip_idempotency_keys from anon, authenticated;
grant select on public.trips, public.trip_days, public.trip_items to authenticated;
```

- **读**：`authenticated` 仅有 `select` 权限，用 `.from('trips').select(...)`
- **写**：表的 insert/update/delete 权限已全部 revoke，**必须走 RPC**（`security definer` 函数）

直接 `.from('trips').insert(...)` 一定失败。写入用 `.rpc()` 调用 `create_trip` / `add_trip_day` / `add_trip_item`。

RLS 策略（同文件 303-308 行）限定 `user_id = auth.uid()`，因此**没有认证会话时读取必然为空**，不是 bug。

## RPC 签名

以下三个基础 RPC 签名摘自 `supabase/migrations/20260821000003_itinerary_rpcs.sql`；最终迁移保留了这些签名，全部返回 `jsonb`：

```
create_trip(
  p_idempotency_key uuid, p_title varchar(80),
  p_timezone text = 'Asia/Shanghai',
  p_start_date date = null, p_end_date date = null,
  p_party_size smallint = 1, p_currency_code char(3) = 'CNY',
  p_budget_limit_minor bigint = null, p_budget_scope text = null,
  p_default_travel_mode text = 'walking')

add_trip_day(
  p_trip_id uuid, p_expected_revision bigint, p_idempotency_key uuid,
  p_local_date date, p_available_start_time time = null,
  p_available_end_time time = null, p_budget_limit_minor bigint = null,
  p_notes varchar(1000) = null)

add_trip_item(
  p_trip_id uuid, p_expected_revision bigint, p_idempotency_key uuid,
  p_trip_day_id uuid, p_item_type text, p_place_id uuid,
  p_title varchar(120), p_planned_start_at timestamptz,
  p_planned_end_at timestamptz, p_time_slot text = 'flexible',
  p_position integer = 0, p_estimated_cost_min_minor bigint = null,
  p_estimated_cost_max_minor bigint = null, p_notes varchar(1000) = null,
  p_place_snapshot jsonb = null)
```

两个必须遵守的调用约定：

1. **幂等键**：每个写 RPC 都需要生成一个 uuid 作为 `p_idempotency_key`。重试必须复用同一个键，否则可能重复创建或重复执行。结果缓存在 `trip_idempotency_keys`。
2. **乐观并发**：除 `create_trip` 外，当前公开的 10 个行程写 RPC 都需传 `p_expected_revision`，取自 `trips.revision`。版本不匹配时 RPC 拒绝写入，客户端应重新读取后重试。

### RPC 签名与最终迁移说明

上面的三个基础 RPC 签名来自初始迁移，仅用于展示参数类型。最终公开 RPC 清单见“最终行程结构补充”；后续迁移增加了节点编辑、改期、删除、批量删除和行程生命周期 RPC，并移除了时区修改与节点级取消/恢复 RPC。

## 字段映射

现有 UI 模型（`trip_models.dart`）与数据库表字段不是一一对应，需要转换层：

| UI 模型 | 数据库来源 | 说明 |
|---------|-----------|------|
| `TripPlan.id` | `trips.id` | uuid |
| `TripPlan.title` | `trips.title` | |
| `TripPlan.destination` | **无对应列** | 已由 `TripMapper.destinationOf` 根据地点项标题在客户端推导，不新增数据库列 |
| `TripPlan.updatedAt` | `trips.updated_at` | |
| `TripDay.date` | `trip_days.local_date` | |
| `TripDay.label` | **无对应列** | 客户端格式化生成（如「周六 · 8 月 22 日」） |
| `TripStop.title` | `trip_items.title` | |
| `TripStop.subtitle` | **无对应列** | 由 `time_slot` + 起止时间拼装 |
| `TripStop.startAt` / `endAt` | `planned_start_at` / `planned_end_at` | `timestamptz`；按 `trips.timezone` 转换展示 |
| `TripStop.type` | `trip_items.time_slot` | `TripStopType` 与数据库 7 个 `time_slot` 值一一对应 |
| `TripStop.itemType` | `trip_items.item_type` | `placeVisit` / `break_` 与 `place_visit` / `break` 对应 |
| `TripStop.note` | `trip_items.notes` | |
| `TripStop.isLocked` | `is_place_locked` / `is_time_locked` / `is_order_locked` | 三个标志按“任一为真”合并为 UI 锁定态 |

数据库与客户端枚举已对齐，无需再猜测映射：

- `TripStopType` 与库 `time_slot`：`breakfast`、`morning`、`lunch`、`afternoon_tea`、`dinner`、`late_night`、`flexible` 一一对应。
- `TripItemType` 与库 `item_type`：`placeVisit` / `break_` 对应 `place_visit` / `break`。
- `TripItemStatus` 与库 `trip_items.status`：`planned`、`completed`、`skipped`。节点不再有 `cancelled` 值。



## 实施步骤

建议按此顺序，每步可独立验证。

### 第 1 步：初始化客户端

`main.dart` 的异步初始化、`SupabaseTripRepository.loadPlan()` 的嵌套读取和基础写入 RPC 已在 2026-08-23 完成；以下代码片段及步骤保留为实现记录，不代表当前待办。

```dart
await Supabase.initialize(
  url: SupabaseConfig.url,
  anonKey: SupabaseConfig.anonKey,
);
```

`SupabaseConfig`（`lib/app/config/supabase_config.dart`）已备好 `url` / `anonKey` / `isConfigured`，通过 `String.fromEnvironment` 读取，无需改动。未配置时应给出可见提示而非静默失败。

### 第 2 步：认证会话

没有会话则 RLS 下读取恒为空，此步不可跳过。最小实现可用邮箱注册/登录（服务端 `GOTRUE_MAILER_AUTOCONFIRM=true`，无需邮箱验证即可登录）。

需处理会话持久化与过期刷新（`supabase_flutter` 默认已含本地持久化）。

### 第 3 步：只读查询（已完成）

`SupabaseTripRepository.loadPlan()` 已使用单次嵌套查询避免 N+1；以下为实现时采用的查询形状：

```dart
.from('trips')
.select('*, trip_days(*, trip_items(*))')
.order('local_date', referencedTable: 'trip_days')
.order('position', referencedTable: 'trip_items')
```

保留现有 `TripRepositoryException` 与 `TripRepositoryErrorKind`（`unavailable / unauthenticated / network / conflict`），把 PostgREST 错误映射到对应 kind，UI 已有分支处理。

`InMemoryTripRepository` 和 `DemoTripRepository` 应保留，用于测试与无网络演示。

### 第 4 步：写入 RPC（已完成）

基础 `create_trip` / `add_trip_day` / `add_trip_item` 封装已经实现；后续节点与行程生命周期 RPC 也已按同一幂等键和 revision 约定接入。

### 第 5 步：注入配置

```
--dart-define=SUPABASE_URL=http://192.144.135.246:8000
--dart-define=SUPABASE_ANON_KEY=<anon key>
```

anon key 存于服务器 `/opt/savorseek/.env` 的 `ANON_KEY`（该 key 设计上可公开，随客户端分发）。**切勿使用 `SERVICE_ROLE_KEY`**，它绕过全部 RLS。

项目已有 `--dart-define-from-file` 惯例（参见 `amap.env`），建议沿用同一方式管理，注意 `.gitignore` 已忽略 `*.env`。

## 明文 HTTP 的影响

当前 `SUPABASE_URL` 只能用 `http://`，服务端尚未配置 HTTPS。这带来两个必须处理的问题：

1. **平台限制**：Android 需在 manifest 声明 `android:usesCleartextTraffic="true"`，iOS 需配置 ATS 例外。否则请求会被系统直接拦截，且报错信息容易被误判为服务端故障。
2. **安全风险**：anon key、用户密码、JWT 全部明文传输，可被中间人截获。

建议在正式联调前先为服务端配置域名 + HTTPS，可省掉上述平台配置，也消除凭据泄露风险。若暂时保持 HTTP，应仅限开发环境。

## 验证方式

服务端可用性已确认，因此联调失败时应优先怀疑客户端。可用以下命令确认服务端仍正常（`<ANON>` 取自服务器 `.env`）：

```bash
# 应返回 200
curl -s -o /dev/null -w "%{http_code}\n" \
  -H "apikey: <ANON>" http://192.144.135.246:8000/rest/v1/

# 注册并取得 JWT
curl -s -X POST -H "apikey: <ANON>" -H "Content-Type: application/json" \
  -d '{"email":"dev@example.com","password":"<password>"}' \
  http://192.144.135.246:8000/auth/v1/signup

# 用上一步的 access_token 查询，应返回 []
curl -s -H "apikey: <ANON>" -H "Authorization: Bearer <JWT>" \
  "http://192.144.135.246:8000/rest/v1/trips?select=id"
```

客户端侧建议补充的测试：

- Repository 单元测试：用 mock 覆盖成功、未认证、网络错误、revision 冲突四种路径
- 字段映射转换的单元测试，尤其枚举映射与时区处理
- 端到端：注册 → 创建行程 → 添加日期与行程项 → 读回校验

## 已决策事项

原文列出的三项待确认已在 2026-08-23 决策并实现：

1. **`TripPlan.destination`**：不新增数据库列，由地点项标题在客户端推导（取前两项，超出时附「等 N 处」）。见 `TripMapper.destinationOf`。
2. **枚举映射**：`TripStopType` 改为与库 `time_slot` 的 7 个取值一一对应，并新增 `TripItemType` 对应 `item_type`。有损映射会让读回的行程在写回时丢失原始时段，双向转换必须恒等。
3. **`isLocked` 合并规则**：三个锁定标志按「任一为真」合并，锁图标表达「此项受保护」。写入侧仍按具体操作分别设置。

## 实施中发现的服务端约束

以下两项原文未提及，实测才暴露：

1. **`place_visit` 必须带地点快照**：库端 trigger（`itinerary_schema.sql:235-260`）要求新建 `place_visit` 带非空 `place_id` 与合规 `place_snapshot`——`schema_version` 必须为 1、`name` 非空、经纬度成对出现且给出时 `coordinate_system ∈ {gcj02, wgs84}`；已有项因地点删除或合并可由外键 `ON DELETE SET NULL` 使 `place_id` 为空，但必须保留快照；`break` 项两者都不能带。违反时返回 `23514`。客户端已加本地前置校验并提供 `PlaceSnapshot` 封装。
2. **revision 冲突行为**：基础迁移最初使用 SQLSTATE `40001`，后由 `20260823000004_itinerary_conflict_errcode.sql` 改为 `P0002`。该修正及后续行程迁移已应用并通过线上核对，客户端应将 `P0002` 映射为可恢复的 revision 冲突。

## 剩余遗留问题

1. 服务端 HTTPS 尚未配置。已加 Android `usesCleartextTraffic` 与 iOS 按域 ATS 例外作为开发期妥协，配好域名与 HTTPS 后必须移除。
2. GoTrue 当前开放注册且邮箱免验证，正式环境需收紧。
3. 写入 RPC 已封装，创建行程、添加日期/节点、编辑、改期、删除、完成和取消路径已接入；后续新增或调整 RPC 时应同步更新本文件的最终 RPC 清单。
4. 跨时区行程展示已按 `trips.timezone` 列格式化；客户端保留已知时区数据集的兼容映射。
5. 联调数据清理和迁移是否已同步到目标实例，必须以服务器上的 `supabase_migrations.schema_migrations` 查询及核对脚本结果为准。

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-08-23 | 初始版本：记录未对接状态、服务端已就绪能力与实施步骤 |
| 1.1 | 2026-08-23 | 更新为已对接状态；补记枚举、destination、锁定标志的决策结果，以及实测发现的地点快照必填约束与 revision 冲突 504 缺陷 |
| 1.2 | 2026-08-28 | 对齐最终行程迁移与自建实例核对结果：补充最终 RPC 清单、移除节点 `cancelled` 与时区修改能力的说明，并更新已过时的实现状态 |