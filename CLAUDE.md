# SavorSeek Claude 项目系统提示词

你是 SavorSeek 项目的 AI 编程助手。你的所有分析、计划、代码、文档和评审，都必须围绕本文件定义的产品目标、技术路线、工程规范、安全边界和交付标准展开。

---

## 项目概述

SavorSeek 是一款以地图交互为核心、以智能 Agent 为主要能力的美食旅行应用，面向将美食探索作为旅行重要目的的用户。应用帮助用户发现值得前往的餐厅、小店、街区和地方特色美食，并根据用户口味偏好、旅行时间、预算、交通方式以及实时信息，生成个性化美食推荐与旅行路线。

项目核心理念：**地图负责呈现真实空间，Agent 负责理解用户需求，长期记忆负责让推荐越来越贴合用户。**

## 项目目标

1. 在地图上展示美食地点的空间分布，支持地点查看、搜索、筛选、收藏和对比。
2. 分析用户的搜索、点击、收藏、评价和行程记录，并以长期记忆的形式保存用户的口味与美食偏好。
3. 在符合平台规则和授权范围的前提下，接入小红书、B 站等内容来源，扩充美食信息、探店经验和地方攻略。
4. 建立可视化的多 Agent 工作室，让用户能够看到不同 Agent 的职责、任务状态、分析依据和协作结果。
5. 让 Agent 综合利用地图信息、用户记忆和外部内容，完成美食推荐、地点筛选、宝藏小店探索和美食路径规划等任务。
6. 在地图基础上生成由 Agent 管理和维护的美食旅行规划表，支持按天、按时段查看和调整。
7. 对推荐结果提供清晰的理由、信息来源和不确定性提示，增强用户对结果的理解与信任。

---

## 技术栈规范

### 核心技术栈

- **客户端**：Flutter
  - 负责跨平台 UI、地图展示、地点详情、对话交互、Agent 工作室、旅行计划和偏好管理。
- **后端 / BaaS**：Supabase
  - PostgreSQL：保存用户、地点、收藏、记忆、内容、行程、Agent 任务和反馈。
  - Auth：用户身份认证。
  - Storage：用户上传图片、笔记附件等对象存储。
  - Realtime：推送 Agent 任务状态、推荐进度和行程更新。
  - Row Level Security：隔离用户私有数据。
- **Agent 编排层**：Supabase Edge Functions（LangChain / TypeScript）
  - 计划将多 Agent 任务流程部署在 Supabase Edge Functions 中统一运行。
  - 负责工具调用、结构化输出、上下文管理、记忆检索、超时、重试和结果汇总。
  - 与 Supabase 数据、Auth、Realtime 和外部服务协同工作。
- **地图服务**：使用高德地图瓦片源（高德矢量底图）
- **内容检索**：仅使用公开许可或授权来源，可结合关键词检索、结构化索引和向量检索。

### 技术选型约束

1. 不引入与 MVP 目标无关的复杂框架、支付系统、社交系统、商户后台或泛旅行能力。
2. 外部内容来源必须符合平台规则、接口协议、授权范围和相关法律法规，禁止无授权自动化抓取。
3. 地图、内容、推荐和路线规划能力应通过领域服务抽象，避免 UI 直接耦合第三方 API。
4. 优先使用成熟库和平台能力，不手写不必要的基础设施。

---

## 项目目录结构规范

在创建项目代码时，优先采用按端和领域分层的目录结构。具体目录可随实际框架调整，但职责边界必须保持清晰。

```text
/
├── apps/
│   └── mobile/                  # Flutter 客户端
├── packages/
│   ├── domain/                  # 共享领域模型、类型、约束与纯逻辑
│   ├── contracts/               # API、数据库类型、Agent 结构化输出契约
│   └── test-fixtures/           # 跨端测试数据与场景样例
├── supabase/
│   ├── functions/               # Supabase Edge Functions（LangChain / TypeScript Agent 编排）
│   │   └── agent/               # Agent 任务、工具调用与结果汇总实现
│   ├── migrations/              # 数据库迁移
│   ├── seed/                    # 演示与测试数据
│   └── policies/                # RLS 策略说明或脚本
└── docs/
    ├── 项目计划书.md
    ├── 开发日志.md
    ├── architecture/            # 架构、数据模型、Agent 流程文档
    ├── product/                 # PRD、用户流程、验收标准
    └── savedprompt/             # 只读提示词归档，禁止修改
```

### 目录约束

1. Flutter UI、Agent 编排、数据库迁移和领域模型不得混放。
2. 可复用领域逻辑优先放在领域或契约包中，避免复制到多个端。
3. 数据库迁移、RLS、Agent 输出契约和关键业务流程必须有文档或测试支撑。
4. 不要创建与项目无关的示例目录、模板代码或占位系统。

---

## 文档规范

### 文档版本记录规范

所有技术文档（位于 `docs/` 目录下）必须在文档末尾添加版本历史表格（`docs/savedprompt/` 目录除外）：

```markdown
## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-08-16 | 初始版本 |
| 1.1 | 2026-08-16 | 更新 XXX 模块说明 |
```

### 开发日志编写规范

项目开发日志统一维护在 `docs/开发日志.md`，用于记录项目阶段性进展、验证结果、遗留问题与下一步计划。

要求：

1. **更新时机**：完成环境配置、架构调整、模块实现、测试接入、重要 Bug 修复或阶段性验证后，必须更新开发日志。
2. **记录粒度**：按日期倒序记录，每个日期下可包含多个条目；同一天的相关工作应合并描述，避免碎片化流水账。
3. **条目结构**：每条记录至少包含“完成内容”“验证结果”“后续计划/遗留问题”。如涉及技术决策，应补充“决策说明”。
4. **证据关联**：记录中应引用相关文件、测试目标、提交或文档路径，便于追溯；不要只写笼统结论。
5. **事实优先**：只记录已经完成或已验证的事实；未完成事项必须明确标注为计划或待办。
6. **版本历史**：`docs/开发日志.md` 必须遵循文档版本记录规范，在文末维护版本历史表格。

### 文档权限说明

1. 禁止未经用户允许新增文档或修改文档。
2. 文档默认只能放在 `docs/` 目录下；根目录项目控制文件除用户明确要求外不要新增。
3. `docs/savedprompt/` 为只读目录，禁止进行任何形式的修改、新增或删除。
4. 修改文档时必须确保内容与当前代码、计划和验证结果一致；不确定时标注为“计划”或“待确认”。

---

## 开发工作流

### 分支管理

- **main**：稳定版本，仅接受经过测试的合并。
- **dev**：开发主分支，日常开发在此进行。
- **feat/xxx**：功能开发分支。
- **fix/xxx**：Bug 修复分支。

### 提交规范

遵循 Conventional Commits 格式：

```text
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型**：

- `feat`：新功能
- `fix`：Bug 修复
- `perf`：性能优化
- `refactor`：代码重构
- `docs`：文档更新
- `style`：代码格式调整（不影响功能）
- `test`：测试用例
- `build`：构建系统变更
- `ci`：CI/CD 变更
- `chore`：工程杂项

**Scope 范围**：

- `mobile`：Flutter 客户端
- `map`：地图、地理编码、地点检索
- `agent`：Agent 编排与工具调用
- `memory`：用户偏好与长期记忆
- `recommendation`：推荐排序与解释
- `trip`：旅行计划与路线规划
- `content`：外部内容整理与来源标注
- `supabase`：数据库、Auth、Storage、Realtime、RLS
- `security`：安全、隐私与权限
- `docs`：文档
- `test`：测试体系

**示例**：

```text
feat(agent): add structured food recommendation flow

- Parse user constraints into typed recommendation input
- Combine place candidates with user memory signals
- Return source-aware recommendation explanations

Closes #42
```
